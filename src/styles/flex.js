import styled from "styled-components";
import { flexbox, layout, display, typography } from "styled-system";

export const Flex = styled.div`
  display: flex;
  ${layout};
  ${flexbox};
  ${display};
  ${typography};
`;
