import React from 'react';
import Navigation from "../Navigation";
import {Container} from "../HomeSearchBar/styled";


class Cities extends React.Component {
    render() {
        return (
            <Container>
                <Navigation/>
            </Container>
        );
    }
}

export default Cities;

